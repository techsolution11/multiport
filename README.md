# multiport
```
apt update && apt upgrade -y && apt install -y wget screen && wget -q https://raw.githubusercontent.com/techsolution11/multiport/main/setup.sh && chmod +x setup.sh && screen -S setup ./setup.sh
```
# netflix
```
apt update -y && apt install sudo && sudo apt install -y wget && sudo apt install -y bzip2 gzip coreutils screen curl && wget https://raw.githubusercontent.com/techsolution11/multiport/main/netflix.sh && chmod +x netflix.sh && ./netflix.sh
```
